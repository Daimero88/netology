version: 3
projects:
  - name: infra
    dir: infrastructure
    workspace: default
    autoplan:
      enabled: true
      when_modified:
        - "*.tf"
        - "**/*.tf"
        - "*.tfvars"
        - "**/*.tfvars"
    apply_requirements:
      - approved
      - mergeable
    workflow: default
